# Application Name
spring.application.name=isthereanyone

# Server Port
server.port=8080

# ================================
# NEON POSTGRESQL DATABASE CONFIG
# ================================
# Ganti dengan credentials dari Neon Console Anda
spring.datasource.url=jdbc:postgresql://ep-empty-lake-a11dl8px-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_up6Kk5nydRNr
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Connection Pool (HikariCP)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000

# CORS - Allow game client connections
# Tambahkan origin game client jika diperlukan

